# BeeLlama v0.4.0 merge worklog

- 2026-07-09: Pinned the upstream merge source to `049326a00025d00b08cc188ed716b681e984a3f8` from the local detached worktree. Confirmed the fork base was clean at `a12d7e228046b06faa6e17d7713dc50db8c94d11`, created `v0.3.2-legacy`, and created branch `v0.4.0`. The plan's separate-worktree precaution was unnecessary because this checkout was already on the clean stated base; using it preserves the supplied build-script paths.
- 2026-07-09: The baseline CUDA build remained active while implementation began. To avoid a source/build race, created `C:\Users\anbee\projects\beellama-v0.4.0` on `v0.4.0-implementation`; implementation commits will be fast-forwarded into `v0.4.0` after the baseline build exits, before final validation with the supplied build script.
- 2026-07-10: Replaced the retained recurrent-memory DFlash replay implementation with the pinned upstream version. The first post-merge CPU build exposed its deleted `dflash-profile.h` dependency; D1 explicitly parks direct replay, while KVarN only needs the generic composite-memory hooks.
- 2026-07-10: Kept iSWA child caches polymorphic for KVarN and added an explicit checked standard-cache boundary in DSV4. DSV4 always constructs standard children, but upstream's direct concrete assumptions are no longer valid once KVarN iSWA support is retained.
- 2026-07-10: Resolved the upstream `Q2_0` format collision with C10's default: Bee's 32-element signed-max cache format is now `GGML_TYPE_Q2_0S`, while the user-facing `q2_0` cache argument continues to select it. Upstream's 64-element `GGML_TYPE_Q2_0` remains available for weights.
- 2026-07-10: Chose C8(a), an offline GGUF rewrite. A real legacy Qwen3.6 IQ4_XS DFlash draft (58 tensors) converted to upstream `dflash`, then reopened with every tensor type and SHA-256 payload checksum verified. The script promotes the legacy mask token to `tokenizer.ggml.mask_token_id` and writes upstream tensor names without requantization.
- 2026-07-10: Ported C9 because upstream's task queue made it a small extension of the retained loop-guard/reasoning-budget hook. It uses the existing sampler force-end path and exposes no fork speculative verifier APIs.
- 2026-07-10: Reapplied two general correctness fixes that the merge had only half retained: F16 CPU `OUT_PROD` now has both implementation and workspace sizing, and CUDA cuBLAS GEMM exits before dispatch for empty tensors. Targeted regressions pass.
- 2026-07-10: The first CUDA default build found two merge-artifact syntax defects before any KVarN kernel compiled: an accidental captured tool-output line in the isolated dispatcher and the missing declaration prefix for `flash_attn_ext_f16_iter`. Removed/restored them directly; no algorithm change.
- 2026-07-10: Ceded orphaned DFlash-mmproj deltas in `tools/mtmd`, removed dead public meta/model introspection declarations, and removed a UI helper whose production consumer was lost to the upstream merge. This restores the pinned upstream surface outside the ratified keeper set.
- 2026-07-10: A second full-delta keeper audit found merge omissions outside the feature keyword scan. Restored Dream 3B/8B/14B model-size mappings, Qwen3Next per-layer KV-head reshaping, the Bee release workflow and CUDA 13.1 assets, fork security/AGENTS guidance, and Bee OCI metadata. Added one static merge-keeper test to guard these files together.
- 2026-07-10: Restored the complete guarded Vulkan KVarN store path (pipeline, push constants, dispatch, support predicate, and registry hook). The shader generator alone was insufficient: without the backend integration, `kvarn_store.comp` was dead code. Vulkan still reports native KVarN cache support as false until its FlashAttention consumes KVarN views.
- 2026-07-10: Windows CUDA 13.1 sm_86 default-policy build passed after the CUDA syntax repair (1070 targets, 27m15s), and the post-audit incremental build passed in 9s. Non-native configure evidence identified only missing platform SDKs: HIP package, MUSA toolkit, and Apple Foundation; Vulkan SDK installation/build is handled separately.
- 2026-07-10: Full Vulkan 1.4.350 build passed (1078 targets). The focused Vulkan backend-op suite passed all 15,046 cases; CUDA `test-kvarn` passed its complete 36-pair matrix, and Compute Sanitizer reported zero errors on the native prefill reproducer.
- 2026-07-10: Real Qwen and Gemma plain/KVarN CLI smokes exposed two merge omissions rather than KVarN math bugs: CUDA KVarN MMA had lost its F16 fixup workspace allocation, and the upstream graph rebase had dropped KVarN domain/rotation/SWA-index plumbing. Restoring those current-API paths removed the sanitizer fault, fixed Qwen output, and removed Gemma's null-index crash.
- 2026-07-10: Upstream DFlash initially crashed during graph reservation because the merge had incorrectly added `LLM_ARCH_DFLASH` to the no-memory architecture list. The pinned upstream implementation routes DFlash through the ordinary KV-cache constructor. A red/green keeper test and real converted-draft runs now cover both default fitting and `-fit off`.
- 2026-07-10: The first 4K/raw-corpus KLD diagnostic (`Qwen 0.032389`, `Gemma 0.487946`) was not comparable to the plan's scorecard. Re-running the exact historical shapes produced Qwen ctx65536/chunks1 KVarN4 `0.002400` and Gemma plain-chat ctx16384/chunks8 KVarN4 `0.402575` versus q5_0 `0.415296`, all at ub256.
- 2026-07-10: The matched server gate measured legacy DFlash at 84.46 median tok/s and upstream DFlash at 73.81 tok/s (`-12.61%`), with acceptance 43.1% versus 40.1%. This is the plan's 10-25% ship-and-flag band. Live profit adaptation transitioned 0 -> 8; `/props`, `/slots`, `/metrics`, forced loop-guard stop, and realtime reasoning control all passed.
- 2026-07-10: G3/G5 artifact limits are explicit: `D:\models` has no Qwen3.6 MoE target GGUF and no `Qwen3.6-27B-Q5_K_S-MTP.gguf`, so the MoE generation leg and `--sm tensor`/`draft-mtp` B8 re-test cannot be executed locally. Linux CUDA also cannot run because WSL has no distribution and Docker is absent; HIP, MUSA, and Metal configure attempts stop at their missing platform SDKs rather than project source.
- 2026-07-10: Removed the stale March multi-GPU Turbo3 benchmark report after the residual-trace audit; Turbo spellings now remain only in compatibility redirects, removal tests, and release history.
- 2026-07-11: Replaced G4's byte-identical output comparison with operational invariants: nonempty target output plus generated and accepted draft tokens. `tmp/v040-evidence/server-gates-20260710-163652/fixed-results.json` preserves the evidence: even the legacy binary's plain and DFlash outputs differ for identical prompts (for example, "Tests that ensure" versus "Tests to ensure"), while legacy and v0.4 DFlash both generated and accepted drafts. Exact bytes are therefore not a stable cross-build correctness oracle.
- 2026-07-11: Kept the `speculative-simple` empty-draft accept guard and flagged the API behavior as an upstream PR candidate. With the guard removed, a real Gemma 4 12B `draft-simple` run forced to produce zero draft tokens failed at `common/speculative.cpp:2626: GGML_ASSERT(impl)`; the identical guarded run exited 0 with `n_drafted = 0` and `n_accept = 0`. Evidence is under `tmp/v040-evidence/postmerge/speculative-simple/`.
- 2026-07-11: Resolved C7 by extending upstream's per-pair CUDA vec-instance mechanism rather than replacing it. Pinned upstream `049326a00` contains 49 `fattn-vec-instance-*.cu` files, so the plan premise that upstream had eliminated vec instances was false; generated Bee additions and the isolated KVarN dispatcher preserve the upstream compilation architecture.
